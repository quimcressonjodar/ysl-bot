# Memory Index

- [Faking a smooth price move on a time-based chart](fake-chart-timestamp-spacing.md) — reshaping price values isn't enough; timestamps must be spread across real chart time too, or it still renders as a vertical line.
- [Discord avatar downloads](discord-avatar-downloads.md) — use discord.py's native Asset.read() instead of a separate raw CDN request.
- [Nested Git repository access](nested-git-repository.md) — push bot changes from its own repository when workspace-level Git helpers cannot detect its remote.
